name: fetch
type: structure

motifs:
  - name: fetch-web

input_schema:
  type: object
  required: [url]
  properties:
    url:
      type: string

output_schema:
  type: object
  properties:
    url:
      type: string
    content:
      type: string

resources: {}
